machine:
  node:
    version: 6.9.1
  environment:
      PATH: "${PATH}:${HOME}/${CIRCLE_PROJECT_REPONAME}/node_modules/.bin"
dependencies:
  override:
    - yarn
    - yarn add g ionic
  cache_directories:
    - ~/.cache/yarn
deployment:
  master:
    branch: master
    owner: platanus
    commands:
      - yarn run build
      - ionic login
      - ionic upload
  release:
    tag: /v[0-9]+(\.[0-9]+)*/
    owner: platanus
    commands:
      - echo "should run ionic package"
test:
  override:
    - echo "no tests :("
