machine:
  node:
    version: 6.9.1
  environment:
      PATH: "${PATH}:${HOME}/${CIRCLE_PROJECT_REPONAME}/node_modules/.bin"
dependencies:
  override:
    - yarn
    - yarn add g ionic
    - yarn run build
    - ionic login
    - ionic upload
  cache_directories:
    - ~/.cache/yarn
test:
  override:
    - echo "no tests :("
